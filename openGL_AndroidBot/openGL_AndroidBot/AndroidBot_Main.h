#pragma once
#include<stdlib.h>		// 필수 헤더파일들
#include<stdio.h>
#include<windows.h>
#include<mmsystem.h>	// MMSystem 헤드파일은 window.h보다 뒤에 선언해야 함!
#include<GL/glut.h>
#include<GL/gl.h>
#include<GL/glu.h>
#include <math.h>

//int a = 0; // x축 기준(값이 1일 때 x축을 기준으로 회전)
//int b = 0; // y축 기준(값이 1일 때 y축을 기준으로 회전)
//int c = 0; // z축 기준(값이 1일 때 z축을 기준으로 회전)
